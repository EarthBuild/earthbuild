
steps:
  - label: Test
    commands:
      - ./.buildkite/tests.sh
    env:
      EARTHBUILD_INSTALL_ID: "earthbuild-buildkite-macos"
      EARTHBUILD_OS: "darwin-m1"
    agents:
      os: macOS
      arch: arm64
    timeout_in_minutes: 55
