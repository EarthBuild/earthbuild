name: CI

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'docs/**'
      - '**.md'
      - '.github/renovate.json5'
      - '.github/CODEOWNERS'
      - 'LICENSE'
  pull_request:
    branches: [ main, get-ci-working ]

permissions: write-all

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  fast-checks:
    name: Fast Checks
    uses: ./.github/workflows/ci-fast-checks.yml
    secrets: inherit

  docker:
    name: Docker
    needs: fast-checks
    uses: ./.github/workflows/ci-docker-ubuntu.yml
    secrets: inherit

  podman:
    name: Podman
    needs: fast-checks
    uses: ./.github/workflows/ci-podman-ubuntu.yml
    secrets: inherit

  next:
    name: Next
    needs: fast-checks
    uses: ./.github/workflows/ci-earthly-next-docker-ubuntu.yml
    secrets: inherit
