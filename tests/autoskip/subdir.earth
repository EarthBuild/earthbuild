VERSION 0.7
PROJECT test/project

foo:
  FROM alpine:3.24.2
  RUN echo hi from foo

deps:
  FROM alpine:3.24.2
  RUN touch /setup

bar:
  FROM +deps
  RUN echo hello from bar

baz:
  BUILD ./subdir+test

all:
  BUILD +foo
  BUILD +bar
  BUILD +baz
