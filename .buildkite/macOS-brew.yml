
steps:
  - label: Test
    commands:
      - bash ./.buildkite/brew-upgrade-test.sh
    env:
      EARTHBUILD_INSTALL_ID: "earthbuild-buildkite-macos"
    agents:
      os: macOS
      arch: amd64
    timeout_in_minutes: 55
