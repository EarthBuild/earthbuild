
steps:
  - label: Test WSL
    commands:
      - ./.buildkite/tests.sh
    key: wsl
    env:
      FORCE_COLOR: 1
      EARTHLY_OS: linux
      EARTHLY_CONFIG: "./.buildkite/earthly-config-win.yml"
    agents:
      os: wsl2
    timeout_in_minutes: 55
