name: Misc Tests 2

on:
  workflow_call:
    inputs:
      BUILT_EARTHBUILD_PATH:
        required: true
        type: string
      BINARY:
        required: true
        type: string
      SUDO:
        type: string
        required: false
      RUNS_ON:
        required: true
        type: string
      SKIP_JOB:
        required: false
        type: boolean
        default: false
      USE_SATELLITE:
        required: false
        type: boolean
        default: false
      SATELLITE_NAME:
        required: false
        type: string


jobs:
  misc-tests-2:
    if: ${{!inputs.SKIP_JOB}}
    runs-on: ${{inputs.RUNS_ON}}
    permissions:
      contents: read
      packages: read
    env:
      FORCE_COLOR: 1
      EARTHBUILD_TOKEN: "${{ secrets.EARTHBUILD_TOKEN }}"
      EARTHBUILD_INSTALL_ID: "earthbuild-githubactions"
      # Used in our github action as the token - TODO: look to change it into an input
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: ./.github/actions/stage2-setup
        with:
          GHCR_USERNAME: ${{ github.actor }}
          GHCR_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
          EARTHBUILD_TOKEN: "${{ secrets.EARTHBUILD_TOKEN }}"
          built_earthbuild_path: "${{ inputs.BUILT_EARTHBUILD_PATH }}"
          BINARY: "${{ inputs.BINARY }}"
          SUDO: "${{ inputs.SUDO }}"
      - name: Set EARTHBUILD_VERSION_FLAG_OVERRIDES env
        run: |-
          set -euo pipefail
          EARTHBUILD_VERSION_FLAG_OVERRIDES="$(tr -d '\n' < .earthbuild_version_flag_overrides)"
          echo "EARTHBUILD_VERSION_FLAG_OVERRIDES=$EARTHBUILD_VERSION_FLAG_OVERRIDES" >> "$GITHUB_ENV"
      - name: Run linux-amd64 specific tests
        run: ${{inputs.SUDO}} ${{inputs.BUILT_EARTHBUILD_PATH}} --ci -P ./tests+ga-linux-amd64
      - name: Execute earthbuild ${{inputs.BINARY}} command
        run: (cd tests/docker && ${{inputs.SUDO}} ../../${{inputs.BUILT_EARTHBUILD_PATH}} docker-build --tag examples-test-docker:latest . && diff <(docker run --rm examples-test-docker:latest) <(echo "hello dockerfile") )
      - name: Execute private image test (earthbuild Only) # TODO move to separate workflow
        run: ${{inputs.SUDO}} ${{inputs.BUILT_EARTHBUILD_PATH}} --ci ./tests+private-image-test
        if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
      - name: Execute save images test
        run: frontend=${{inputs.BINARY}} ./tests/save-images/test.sh
      - name: Experimental tests
        run: |-
          ${{inputs.SUDO}} ${{inputs.BUILT_EARTHBUILD_PATH}} --ci -P ./tests+experimental
      - name: Test buildkit info-level logging
        run: ${{inputs.SUDO}} ${{inputs.BINARY}} logs earthbuild-buildkitd 2>&1 | grep 'running server on'
      - name: Test for uncommitted generated code
        run: |-
          ${{inputs.SUDO}} ${{inputs.BUILT_EARTHBUILD_PATH}} --ci -P ./ast/parser+test-not-committed
      - name: Buildkit logs (runs on failure)
        if: ${{ failure() }}
        run: ${{inputs.SUDO}} ${{inputs.BINARY}} logs earthbuild-buildkitd || true
